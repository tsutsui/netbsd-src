#	$NetBSD$

PROG=		boothfs
NOMAN=		# defined
BINMODE=	444

SRCS=		# generated from ${TOOL_MACPPCMKBOOTHFS}

${PROG}:
	${TOOL_MACPPCMKBOOTHFS} ${.TARGET}

CLEANFILES+=	${BOOTHFS}

.include <bsd.prog.mk>
